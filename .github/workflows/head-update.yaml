name: Build
on:
  push:
  pull_request:

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      mode: snapshot
    permissions:
      contents: read
      packages: write
      id-token: write
